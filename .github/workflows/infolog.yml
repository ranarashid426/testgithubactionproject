name: infolog exercise 1
on: issues
jobs:
    Getloginfo:
        runs-on: ubuntu-latest
        steps:
            - name: Output the event details
              run: echo "${{toJson(github.event.issue)}}"